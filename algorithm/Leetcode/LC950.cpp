#include <iostream>
#include <vector>

class Solution
{
public:
    std::vector<int> deckRevealedIncreasing(std::vector<int> &deck)
    {
    }
};

int main()
{
    Solution s1;
    std::vector<int> deck = {17, 13, 11, 2, 3, 5, 7};
    s1.deckRevealedIncreasing(deck);
    return 0;
}